name: askaway_openai
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  http: ^1.1.0
  dash_chat_2: ^0.0.20
  get: ^4.6.6
  chat_gpt_sdk: 3.0.9
  win32: ^5.5.1
  file_picker: ^8.0.5

  
dev_dependencies:

  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
